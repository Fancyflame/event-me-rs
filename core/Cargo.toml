[package]
edition = "2018"
name = "event-me"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokio = {version = "1.8", features = ["sync", "macros", "rt-multi-thread", "time"]}
event-me-macros={ path="../macros" }
